/*
 * Code Connect mapping for the (former) Figma `Dropdown menu` component set.
 *
 * STATUS: Missing-in-Figma stub.
 *
 * The original node `54061:36963` that this file targeted is no longer found in the
 * `ANDROID - New` library — `figma connect publish --dry-run` reports "node not found
 * in file". Searching the library for "Dropdown" returns a `Dropdown menu` component
 * (componentKey ca52d1f7b9f2b15ea34c14da9e8b602cf9b585e5) but its node URL has not
 * been provided by the designer yet.
 *
 * Compose side (still in use):
 *   - MegaDropDownMenu(expanded, onDismissRequest, modifier, offset, dropdownItems, onItemClick)
 *
 * Follow-up needed from the designer: provide the new node URL for the dropdown menu
 * component so this file can be revived with a real `@FigmaConnect`.
 *
 * This stub keeps the file in place so the Code Connect dry-run is unblocked.
 */

package mega.android.core.ui.components.dropdown
